import {db} from './firebase-init.js';
import {collection,addDoc,serverTimestamp} from 'https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js';
const form=document.querySelector('#contactForm');
form?.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(form);if(fd.get('website'))return;const btn=form.querySelector('button[type=submit]');btn.disabled=true;const notice=document.querySelector('#contactNotice');try{await addDoc(collection(db,'inquiries'),{name:String(fd.get('name')).trim(),email:String(fd.get('email')).trim(),company:String(fd.get('company')).trim(),department:String(fd.get('department')).trim(),message:String(fd.get('message')).trim(),createdAt:serverTimestamp(),status:'new'});form.reset();notice.textContent='Thanks. Your enquiry has been securely submitted to Navdiva Group.';notice.className='notice success';}catch(err){console.error(err);notice.textContent='Unable to submit right now. Please email corporate@navdiva.com.';notice.className='notice error'}finally{btn.disabled=false}});
