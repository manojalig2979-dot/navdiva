import {db} from './firebase-init.js';
import {collection,addDoc,serverTimestamp} from 'https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js';
const form=document.querySelector('#careerForm');
form?.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(form);if(fd.get('website'))return;const btn=form.querySelector('button[type=submit]');btn.disabled=true;const notice=document.querySelector('#careerNotice');try{await addDoc(collection(db,'applications'),{name:String(fd.get('name')).trim(),email:String(fd.get('email')).trim(),phone:String(fd.get('phone')).trim(),role:String(fd.get('role')).trim(),message:String(fd.get('message')).trim(),createdAt:serverTimestamp(),status:'new'});form.reset();notice.textContent='Application received. Our team will review it and contact you if there is a match.';notice.className='notice success';}catch(err){console.error(err);notice.textContent='We could not submit your application. Please try again later.';notice.className='notice error'}finally{btn.disabled=false}});
